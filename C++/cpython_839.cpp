#include<iostream>
using namespace std;
int soni(int n) {
	int s = 0;
	while(n > 1) {
		while(n > 1) {
			n = n%tub(i);
		}
	}
}	
}
int main()
{
	int n,c = 0;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		c += soni(i);
	}
	cout << c;
}
